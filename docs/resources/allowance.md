---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "polymarket_allowance Resource - polymarket"
subcategory: ""
description: |-
  Manages an on-chain token approval the Polymarket exchange needs to fill orders: an ERC-20 USDC spend allowance or an ERC-1155 CTF operator approval. Requires the provider's private_key and rpc_endpoint.
  ~> This resource submits real Polygon transactions that cost gas. Destroying it revokes the approval (sets the allowance to 0 / operator approval to false).
---

# polymarket_allowance (Resource)

Manages an on-chain token approval the Polymarket exchange needs to fill orders: an ERC-20 USDC spend allowance or an ERC-1155 CTF operator approval. Requires the provider's `private_key` and `rpc_endpoint`.

~> This resource submits real Polygon transactions that cost gas. Destroying it revokes the approval (sets the allowance to 0 / operator approval to false).

## Example Usage

```terraform
# Requires the provider's private_key and rpc_endpoint. Submits real Polygon
# transactions that cost gas.
provider "polymarket" {
  private_key  = var.polymarket_private_key # sensitive
  rpc_endpoint = "https://polygon-rpc.com"
}

locals {
  usdc     = "0x2791Bca1f2de4661ED88A30C99A7a9449Aa84174" # USDC.e
  ctf      = "0x4D97DCd97eC945f40cF65F87097ACe5EA0476045" # Conditional Tokens
  exchange = "0x4bFb41d5B3570DeFd03C39a9A4D8dE6Bd8B8982E" # CTF Exchange
}

# Approve the exchange to spend USDC (unlimited, the default).
resource "polymarket_allowance" "usdc_spend" {
  token   = local.usdc
  spender = local.exchange
}

# Approve the exchange as an operator of the CTF outcome tokens (ERC-1155).
resource "polymarket_allowance" "ctf_operator" {
  token   = local.ctf
  spender = local.exchange
  erc1155 = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `spender` (String) Address granted the approval, e.g. the CTF Exchange or NegRisk Exchange contract.
- `token` (String) Address of the token contract to approve. Typically the USDC token (for ERC-20) or the Conditional Tokens contract (for ERC-1155).

### Optional

- `amount` (String) ERC-20 approval amount in base units (USDC has 6 decimals). When omitted, an unlimited (max-uint256) approval is granted. Ignored when `erc1155` is true.
- `erc1155` (Boolean) Set to `true` to manage an ERC-1155 operator approval (`setApprovalForAll`); leave `false` (default) for an ERC-20 spend allowance.

### Read-Only

- `approved` (Boolean) Whether the approval is currently in place on-chain, as last refreshed. For ERC-20 this is true when the allowance covers the requested amount.
- `transaction_hash` (String) Hash of the most recent approval transaction submitted for this resource.

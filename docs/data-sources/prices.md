---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "polymarket_prices Data Source - polymarket"
subcategory: ""
description: |-
  Fetches the best buy and sell price for several CLOB outcome tokens in a single request.
---

# polymarket_prices (Data Source)

Fetches the best buy and sell price for several CLOB outcome tokens in a single request.

## Example Usage

```terraform
data "polymarket_market" "m" { id = "540817" }

# Best buy/sell for every outcome of the market in one request.
data "polymarket_prices" "all" {
  token_ids = data.polymarket_market.m.clob_token_ids
}

output "prices" {
  value = { for p in data.polymarket_prices.all.prices : p.token_id => "${p.buy}/${p.sell}" }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `token_ids` (List of String) CLOB ERC-1155 token IDs to quote, from markets' `clob_token_ids`.

### Read-Only

- `prices` (Attributes List) Best prices per token, in the same order as `token_ids`. (see [below for nested schema](#nestedatt--prices))

<a id="nestedatt--prices"></a>
### Nested Schema for `prices`

Read-Only:

- `buy` (String) Best buy price (lowest ask), in `[0, 1]`, as a decimal string.
- `sell` (String) Best sell price (highest bid), in `[0, 1]`, as a decimal string.
- `token_id` (String) Token the prices are for.
